# Local Java Progress

Java Reviewer progress will be stored in the browser for v1 instead of requiring accounts or a database. This keeps the project aligned with the current reviewer app and avoids adding authentication before the learning and runner workflows are proven.
