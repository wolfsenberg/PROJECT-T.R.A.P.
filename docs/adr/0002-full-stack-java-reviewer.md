# Full-stack Java Reviewer

We will build the Java Reviewer as a full-stack feature instead of a frontend-only slice. This creates more upfront work because the Java Code Runner needs a backend boundary, deployment plan, and execution controls, but it avoids designing the Java practice UI around a fake runner and gives the project a clearer path to Render deployment.
