# Problem-only Java Runner

We will implement Java code execution as a problem-only runner, not as a free public playground. This keeps the Java Reviewer focused on university-style practice, lets the app grade submissions against predefined tests, and reduces the risk of arbitrary code execution compared with unrestricted user-submitted programs.
