# Standard I/O Java Grading

Java Coding Problems will be graded with standard input/output tests. This matches full-class beginner exercises, keeps the runner contract simple (`Main.java` reads from `System.in` and prints to `System.out`), and avoids the complexity of wrapping arbitrary student classes in hidden method harnesses for v1.
