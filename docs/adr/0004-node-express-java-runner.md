# Node Express Java Runner

The Java runner backend will use Node/Express in a Docker image with a JDK installed. This keeps the web service logic close to the existing TypeScript/JavaScript stack while Docker provides a reproducible Java runtime for Render deployment.
