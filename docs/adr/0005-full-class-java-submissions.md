# Full-class Java Submissions

Java Coding Problems will accept complete Java class submissions instead of method-only answers. This better matches beginner university practice and lets students work with `main`, input, and output, but it means the runner must enforce a predictable `Main` class shape and grade through standard input/output or generated harnesses.
