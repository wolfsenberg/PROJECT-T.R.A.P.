# Monorepo Java Runner

The Java runner backend will live in the same repository as the Next.js frontend, under its own service folder. This keeps the project easier to manage during development while still allowing Render to deploy the backend separately from a subdirectory or Dockerfile.
